const { setupHandlers, emitScoreUpdate, emitLifecycleEvent, emitCommentary, getRoomStats } = require('./handlers');

module.exports = {
  setupHandlers,
  emitScoreUpdate,
  emitLifecycleEvent,
  emitCommentary,
  getRoomStats,
};
